import React from 'react';
import { Link } from 'react-router-dom';
import { ArrowRight, Star, Truck, Shield, Headphones, ArrowLeft } from 'lucide-react';
import { useProducts } from '../context/ProductContext';
import ProductGrid from '../components/Products/ProductGrid';
import ProductCard from '../components/Products/ProductCard';
import { useRef } from 'react';
import FeaturedCategories from '../components/Products/FeaturedCategories';

const Home: React.FC = () => {
  const { getFeaturedProducts, products, categories } = useProducts();
  // const featuredProducts = getFeaturedProducts();
  // Sort products by createdAt descending and take the latest 6
  const latestProducts = [...products]
    .sort((a, b) => new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime())
    .slice(0, 6);
  // Trending products for carousel (first 8)
  const trendingProducts = [...products]
    .sort((a, b) => new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime())
    .slice(0, 8);
  const carouselRef = useRef<HTMLDivElement>(null);

  return (
    <div className="min-h-screen">
      {/* Hero Section */}
      <section className="relative bg-gradient-to-r from-primary to-orange-400 text-white">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 md:py-20">
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-8 md:gap-12 items-center">
            <div>
              <h1 className="text-3xl sm:text-4xl md:text-6xl font-bold mb-4 md:mb-6">
                Shop the Latest
                <span className="block text-primary-light">Tech & Fashion</span>
              </h1>
              <p className="text-base sm:text-lg md:text-xl mb-6 md:mb-8 text-primary-light">
                Discover amazing products at unbeatable prices. Quality you can trust, delivered to your door.
              </p>
              <div className="flex flex-col sm:flex-row gap-3 sm:gap-4 w-full max-w-xs sm:max-w-none">
                <Link
                  to="/products"
                  className="bg-white text-primary px-6 py-3 rounded-lg font-semibold hover:bg-primary-light transition-colors inline-flex items-center justify-center space-x-2 w-full sm:w-auto"
                >
                  <span>Shop Now</span>
                  <ArrowRight className="w-5 h-5" />
                </Link>
                <Link
                  to="/categories"
                  className="border-2 border-white text-white px-6 py-3 rounded-lg font-semibold hover:bg-white hover:text-primary transition-colors w-full sm:w-auto text-center"
                >
                  Browse Categories
                </Link>
              </div>
            </div>
            <div className="relative mt-8 lg:mt-0 flex justify-center">
              <img
                src="https://images.pexels.com/photos/230544/pexels-photo-230544.jpeg?auto=compress&cs=tinysrgb&w=800"
                alt="Shopping"
                className="rounded-lg shadow-2xl w-full max-w-xs sm:max-w-md md:max-w-lg lg:max-w-full h-auto object-cover"
              />
            </div>
          </div>
        </div>
      </section>

      {/* Features Section */}
      <FeaturedCategories />

      {/* Trending Products Carousel */}
      <section className="py-16 bg-white">
        <div className="max-w-7xl mx-auto px-2 sm:px-4 md:px-6 lg:px-8">
          <div className="text-center mb-8 md:mb-12">
            <h2 className="text-2xl md:text-3xl font-bold text-gray-900 mb-3 md:mb-4 flex items-center justify-center gap-2">
              <span className="inline-block bg-primary text-white px-3 py-1 md:px-4 md:py-1 rounded-full text-base md:text-lg">Trending Products</span>
            </h2>
            <p className="text-base md:text-lg text-gray-600">See what's hot right now</p>
          </div>
          <div className="relative">
            <button
              className="hidden sm:flex absolute left-0 top-1/2 -translate-y-1/2 z-10 bg-white shadow rounded-full p-2 hover:bg-primary hover:text-white transition-colors"
              onClick={() => {
                if (carouselRef.current) {
                  carouselRef.current.scrollBy({ left: -320, behavior: 'smooth' });
                }
              }}
              aria-label="Scroll left"
            >
              <ArrowLeft className="w-6 h-6" />
            </button>
            <div
              ref={carouselRef}
              className="flex gap-4 md:gap-6 overflow-x-auto pb-4 no-scrollbar scroll-smooth"
              style={{ scrollSnapType: 'x mandatory' }}
            >
              {trendingProducts.map(product => (
                <div key={product.id} style={{ minWidth: 200, maxWidth: 260, scrollSnapAlign: 'start' }} className="w-5/6 sm:w-auto">
                  <ProductCard product={product} />
                </div>
              ))}
            </div>
            <button
              className="hidden sm:flex absolute right-0 top-1/2 -translate-y-1/2 z-10 bg-white shadow rounded-full p-2 hover:bg-primary hover:text-white transition-colors"
              onClick={() => {
                if (carouselRef.current) {
                  carouselRef.current.scrollBy({ left: 320, behavior: 'smooth' });
                }
              }}
              aria-label="Scroll right"
            >
              <ArrowRight className="w-6 h-6" />
            </button>
          </div>
        </div>
      </section>

      {/* Why Shop With Us */}
      <section className="py-12 md:py-16 bg-gradient-to-r from-orange-50 to-orange-100">
        <div className="max-w-7xl mx-auto px-2 sm:px-4 md:px-6 lg:px-8">
          <div className="text-center mb-8 md:mb-12">
            <h2 className="text-2xl md:text-3xl font-bold text-gray-900 mb-3 md:mb-4">Why Shop With Us?</h2>
            <p className="text-base md:text-lg text-gray-600">We make your shopping experience better</p>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 md:gap-8">
            <div className="bg-white rounded-2xl shadow p-5 md:p-6 flex flex-col items-center">
              <Truck className="w-8 h-8 md:w-10 md:h-10 text-primary mb-3 md:mb-4" />
              <h3 className="text-lg md:text-xl font-semibold mb-1 md:mb-2">Fast Shipping</h3>
              <p className="text-gray-600 text-center text-sm md:text-base">Get your products delivered quickly and reliably to your doorstep.</p>
            </div>
            <div className="bg-white rounded-2xl shadow p-5 md:p-6 flex flex-col items-center">
              <Shield className="w-8 h-8 md:w-10 md:h-10 text-primary mb-3 md:mb-4" />
              <h3 className="text-lg md:text-xl font-semibold mb-1 md:mb-2">Secure Payment</h3>
              <p className="text-gray-600 text-center text-sm md:text-base">Your payments are protected with industry-leading security.</p>
            </div>
            <div className="bg-white rounded-2xl shadow p-5 md:p-6 flex flex-col items-center">
              <Headphones className="w-8 h-8 md:w-10 md:h-10 text-primary mb-3 md:mb-4" />
              <h3 className="text-lg md:text-xl font-semibold mb-1 md:mb-2">24/7 Support</h3>
              <p className="text-gray-600 text-center text-sm md:text-base">Our team is here to help you anytime, anywhere.</p>
            </div>
          </div>
        </div>
      </section>

      {/* Latest Products */}
      <section className="py-12 md:py-16 bg-gray-50">
        <div className="max-w-7xl mx-auto px-2 sm:px-4 md:px-6 lg:px-8">
          <div className="text-center mb-8 md:mb-12">
            <h2 className="text-2xl md:text-3xl font-bold text-gray-900 mb-3 md:mb-4 flex items-center justify-center gap-2">
              <ArrowRight className="w-6 h-6 md:w-7 md:h-7 text-primary inline-block" /> Latest Products
            </h2>
            <p className="text-base md:text-lg text-gray-600">Check out the newest arrivals in our store</p>
          </div>
          <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-3 md:gap-6">
            {latestProducts.map(product => (
              <ProductCard key={product.id} product={product} />
            ))}
          </div>
          <div className="text-center mt-8 md:mt-12">
            <Link
              to="/products"
              className="inline-flex items-center justify-center space-x-2 bg-primary text-white px-6 md:px-8 py-3 rounded-lg font-semibold hover:bg-primary-dark transition-colors w-full sm:w-auto"
            >
              <span>View All Products</span>
              <ArrowRight className="w-5 h-5" />
            </Link>
          </div>
        </div>
      </section>

     

      {/* Newsletter */}
      <section className="py-12 md:py-16 bg-primary text-white">
        <div className="max-w-7xl mx-auto px-2 sm:px-4 md:px-6 lg:px-8 text-center">
          <h2 className="text-2xl md:text-3xl font-bold mb-3 md:mb-4">Stay Updated</h2>
          <p className="text-base md:text-xl mb-6 md:mb-8 text-primary-light">
            Get the latest deals and product updates delivered to your inbox
          </p>
          <div className="max-w-xs sm:max-w-md mx-auto flex flex-col sm:flex-row gap-3 sm:gap-4">
            <input
              type="email"
              placeholder="Enter your email"
              className="flex-1 px-4 py-3 rounded-lg text-gray-900 focus:outline-none focus:ring-2 focus:ring-primary-light"
            />
            <button className="bg-white text-primary px-6 py-3 rounded-lg font-semibold hover:bg-primary-light transition-colors w-full sm:w-auto">
              Subscribe
            </button>
          </div>
        </div>
      </section>
    </div>
  );
};

export default Home;